Build all smart contracts, fix any errors, and report status.

1. Run: `cd contracts && forge build 2>&1`
2. If compilation errors exist, read the error, fix the source file, and rebuild
3. Repeat until clean build
4. Report: number of contracts compiled, any warnings
5. Update PLAN.md if this was a first successful build
